#include <iostream>
#include <cmath>

using namespace std;

int main(){
	
	int n, i = 0, sum = 0, sumador = 0 ;
	
	cin >> n ;
	
	while (i <= n ){
		sumador = (pow(1,n)) * (pow(i,2)) ;
		++i;
		sum += sumador;
		
}
cout << sum << endl ;

		
		
		
	
	}
